<% if signed_in? %>
	<div class="row">
		<aside class="span4">
			<section>
				<%= render 'shared/user_info' %>
			</section>
			<section>
				<%= render 'shared/micropost_form' %>
			</section>
		</aside>

		<div class="span8">
			<h3>微博</h3>
			<%= render 'shared/feed' %>
		</div>
	</div>
<% else %>

<div class="center hero-unit">
<h3 align="center">遇见未来的自己</h3>
<img src="assets/relation.png" alt="这是一张图片"/>
</br>
<%= link_to "马上注册", signup_path, class: "btn btn-large btn-primary" %>
</div>

<% end %>
